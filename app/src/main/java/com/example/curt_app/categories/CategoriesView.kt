package com.example.curt_app.categories

interface CategoriesView {
    fun showElectricCount(listSize : Int)
    fun showMechanicalCount(listSize : Int)
    fun showRawMaterialCount(listSize : Int)
}